/*
 * subset.cpp
 *
 *  Created on: Jun 11, 2014
 *      Author: Lexie
 */
#include<algorithm>
#include<vector>
#include<iostream>

using namespace std;

vector<vector<int> > subsets(vector<int> &s){
      vector<vector<int> > ret;
      vector<int> cRet;
      if(s.size()==0) return ret;
      ret.push_back(cRet);
      cout<<"begin";
      sort(s.begin(), s.end());
      cout<<"hello";
      subsets_helper(s, 0, cRet, ret);
      return ret;
 }

int main(){
      vector<int> test;
      test.push_back(1);
      test.push_back(2);
      test.push_back(3);
     //generate_power_set(test);




